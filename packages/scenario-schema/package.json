{
  "name": "@ontologist/scenario-schema",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Scenario Schema (JSON + Zod) for The Last Ontologist cases. Enforces the human anchor rule (§1.6) at the schema level.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^3.0.0"
  }
}
