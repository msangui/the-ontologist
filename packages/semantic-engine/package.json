{
  "name": "@ontologist/semantic-engine",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Pure-TypeScript deterministic semantic engine (typed property graph + event-sourced assertion log). Zero renderer dependencies — the §18.5 contract.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^3.0.0"
  }
}
